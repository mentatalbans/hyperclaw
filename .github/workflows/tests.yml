name: Test battery

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deterministic:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ matrix.python }}
      - name: Install locked dependencies
        run: |
          python -m pip install uv==0.8.3
          uv sync --locked --extra dev --python "${{ matrix.python }}"
      - name: Run isolated offline battery
        run: make test PYTHON=.venv/bin/python COVERAGE=1
      - name: Build and smoke-install the wheel outside the checkout
        run: |
          uv build
          uv venv --python "${{ matrix.python }}" "$RUNNER_TEMP/hyperclaw-wheel"
          uv pip install --python "$RUNNER_TEMP/hyperclaw-wheel/bin/python" dist/*.whl
          cd "$RUNNER_TEMP"
          "$RUNNER_TEMP/hyperclaw-wheel/bin/hyperclaw" --help
      - name: Save test, timing, and coverage reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: tests-python-${{ matrix.python }}
          path: test-results/
          if-no-files-found: error
          retention-days: 14
