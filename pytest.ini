[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers
junit_family = xunit1
markers =
    integration: real local process and HTTP boundary
    ollama: opt-in live model checks (enable with --run-ollama)
